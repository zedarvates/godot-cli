{
  "name": "godot-cli",
  "version": "0.4.0",
  "description": "CLI tool for controlling Godot game engine - like Playwright, but for games",
  "type": "module",
  "bin": {
    "godot-cli": "./dist/src/cli.js",
    "godot-cli-mcp": "./dist/src/mcp-cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
